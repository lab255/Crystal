{
  "name": "@crystal/relay",
  "version": "0.1.0",
  "description": "Crystal publish relay: Cloudflare Worker + SQLite-backed Durable Object that pairs a published bridge server with remote clients",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250712.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.26.0"
  }
}
