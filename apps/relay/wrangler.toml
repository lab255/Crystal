# Crystal publish relay. Domain is TBD — workers.dev serves until a route is added.
name = "crystal-relay"
main = "src/index.ts"
compatibility_date = "2025-09-01"
workers_dev = true

[[migrations]]
tag = "v1"
new_sqlite_classes = ["BridgeRelayDO"]

[[durable_objects.bindings]]
name = "RELAY"
class_name = "BridgeRelayDO"
