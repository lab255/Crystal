{
  "name": "@crystal/desktop",
  "version": "0.26.0",
  "description": "Crystal desktop app (Tauri shell around the Crystal web UI + bridge server)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@crystal/server": "workspace:*",
    "@crystal/web": "workspace:*",
    "@tauri-apps/cli": "^2.2.0"
  }
}
