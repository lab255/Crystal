{
  "identifier": "default",
  "description": "Baseline for the main window: core APIs (events, channels) for the bridge pipe relay, plus the updater (check/download/install) and process relaunch for in-app auto-update.",
  "windows": ["*"],
  "permissions": ["core:default", "updater:default", "process:default"]
}
