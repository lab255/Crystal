{
  "identifier": "default",
  "description": "Baseline for the main window: core APIs (events, channels) for the bridge pipe relay, the updater (check/download/install) and process relaunch for in-app auto-update, and system notifications for needs-you attention transitions.",
  "windows": ["*"],
  "permissions": ["core:default", "updater:default", "process:default", "notification:default"]
}
